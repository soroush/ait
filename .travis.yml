before_install:
 - sudo apt-get update -qq
 - sudo apt-get install -qq libprotobuf-dev libzmq3-dev xsdcxx
 - autoreconf
language: cpp
compiler:
  - gcc
  - clang
script: 
  - ./configure --enable-precompiled-protocols --enable-silent-rules
  - make
